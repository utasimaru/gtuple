[package]
name = "gtuple"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true # 手続き的マクロとしてビルドする設定

[dependencies]
syn = { version = "2.0", features = ["full"] }
quote = "1.0"
proc-macro2 = "1.0"
